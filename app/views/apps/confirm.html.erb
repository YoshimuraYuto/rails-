<h3>以下の内容で、送信する。</h3>

<%= form_with(model: @app, local: true, url: apps_path) do |form| %>
  <%= form.hidden_field :content %>


  <p>本文:<%= @app.content %></p>
  <%= form.submit  "登録する" %>
  <%= form.submit "戻る", name: 'back' %>
<% end %>
